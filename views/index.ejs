
<div>
  <h2>List of products</h2>
  <ul>
    <% products.forEach( e => { %>
      <li>
        <a href="/products/<%= e._id %>"><%= e.name %></a> from <%= e.user_name %>
      </li>
    <% }) %>
  </ul>
</div>
