
<div>
  <h2><%= title2 %></h2>
  <ul>
    <% products.forEach( e => { %>
      <li>
        <a href="/products/<%= e._id %>"><%= e.name %></a> from <%= e.user_name %>
      </li>
    <% }) %>
  </ul>
</div>
